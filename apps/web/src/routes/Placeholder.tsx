export function Placeholder({ title }: { title: string }) {
  return (
    <section className="hero">
      <div className="hero-content">
        <h1>{title}</h1>
        <p>This screen has not been ported yet.</p>
      </div>
    </section>
  );
}
